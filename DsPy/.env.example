# Ollama configuration (local LLM backend — no API key needed)
# Default Ollama server URL — change only if running Ollama on a different host/port
OLLAMA_BASE_URL=http://localhost:11434

# Optional: force a specific model instead of auto-detection
# OLLAMA_MODEL=llama3.2
